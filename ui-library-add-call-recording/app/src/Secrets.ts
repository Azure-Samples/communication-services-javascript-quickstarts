// Generate a user and token with the "Voice and video calling (VOIP)" scopes.
// and paste the secrets here.
export const ACS_USER_ID = "REPLACE_WITH_USER_ID"
export const ACS_TOKEN = "REPLACE_WITH_TOKEN"